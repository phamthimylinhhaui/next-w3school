/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  images: {
    unoptimized: true,
  },
  // basePath: '/next-w3school',
  // assetPrefix: '/next-w3school',
}

module.exports = nextConfig
